package object speed {

  type NeighborID = String
 
  /**
   * Define when to timeout an entry within table.  
   */
  type ExpireTime = Long

  /**
   * A delay estimation to the neighbor node.
   */
  type SendToDelay = Long

}

